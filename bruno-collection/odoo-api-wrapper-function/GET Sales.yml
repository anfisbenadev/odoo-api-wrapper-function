info:
  name: GET Sales
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/api/sales?code={{code}}"
  params:
    - name: code
      value: "{{code}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
