# odoo-api-wrapper-function
Azure Function for integrating Odoo to PowerBI

To run local dev azure storage environment:
npx azurite --silent --location ~/.azurite --debug ~/.azurite/debug.log

To run the function locally
npx func start